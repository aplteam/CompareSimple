 R←Test_032(stopFlag batchFlag);name1;name2;⎕TRAP
⍝ Exercise "These" with one ref pointing to an ordinary namespace (not expected to work). Uses CompareIt!
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆Failed

 :If 1≢batchFlag

     name1←'#.Old.ADOC'
     name2←'#.New'
     :Trap 0
         ##.CompareSimple.compareTool←'CompareIt!'
         ##.CompareSimple.These name1 name2
         →FailsIf
     :Else
         →PassesIf'"These" cannot process any ordinary namespace: #.New; use `Compare.Merge` instead.'≡1⊃⎕DM
     :EndTrap
     R←∆OK
 :Else
     R←∆NoBatchTest
 :EndIf
