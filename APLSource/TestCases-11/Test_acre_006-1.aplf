 R←Test_acre_006(stopFlag batchFlag);parms;filename;name;⎕TRAP
⍝ Exercise "These" with an invalid and/or empty command space parameter
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←∆Failed

 :If ∆AcreFlag
     parms←⎕NS''
     parms.Unknown←'firlefanz'      ⍝ This is invalid
     filename←'file://',GetFileRoot,'Compare.acre/#.Old.ADOC.623'
     name←'#.Old.ADOC'
     :Trap 11
         parms ##.CompareSimple.These name filename
     :Else
         ⍎('Invalid left argument: Unknown'≢1⊃⎕DM)/'. ⍝ Something is wrong!'
     :EndTrap

     parms←⎕NS''
     parms.Unknown←'firlefanz'      ⍝ This is invalid
     parms.AlsoUnknown←'hubucup'    ⍝ This is also invalid
     filename←'file://',GetFileRoot,'Compare.acre/#.Old.ADOC.623'
     name←'#.Old.ADOC'
     :Trap 11
         parms ##.CompareSimple.These name filename
     :Else
         ⍎('Invalid left argument: AlsoUnknown,Unknown'≢1⊃⎕DM)/'. ⍝ Something is wrong!'
     :EndTrap

     parms←##.CompareSimple.CreateParmsFor_These
     parms.reportFile←#.FilesAndDirs.GetTempFilename''
     parms.componentNo←¯3
     filename←'file://',GetFileRoot,'CompareSimple.acre/#.Old.ADOC.623'
     name←'#.Old.ADOC'
     parms ##.CompareSimple.These name filename
     ∆CheckCompareResult parms.reportFile
     R←∆OK

 :Else
     R←∆Inactive
 :EndIf
